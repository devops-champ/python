#tuples use parenthesis
#they are immutable ie once an element is assigned it cannot be changed

some_data = (
    'one',
    2,
    3.5686596
    
)

print(some_data[1])

#index method

t = ('a', 'a', 'a', 'b', 'b', 'c', 'd')

print(t.index('b'))

#count method

print(t.count('d'))